import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { ContentBreak } from './index.jsx';

<Meta
    title="Elements/ContentBreak"
    component={ContentBreak}
    argTypes={{
        isBig: {
            name: 'Is big',
            description: 'Should it be a big break?',
            controls: {
                type: 'boolean'
            }
        }
    }}
    parameters={{
        jest: []
    }}
/>

# Content Break

Content breaks are used to visually seperate sections of content.

<Canvas>
    <Story name="Large break" args={{
        isBig: true
    }}>
        {(args) => <ContentBreak {...args } />}
    </Story>
</Canvas>

<Canvas>
    <Story name="Small break" args={{
        isBig: false
    }}>
        {(args) => <ContentBreak {...args } />}
    </Story>
</Canvas>

<ArgsTable of={ContentBreak} />

## When to use content breaks

Use content breaks to break up a page that has different content of information.

## When not to use a content break

Do not use content breaks to break up paragraphs within a body of text or divide up forms.

## How to use content breaks

They should be used to:

- thematically divide up sections of content, such as services, news and legislation
- show a divide between a list of clickable items, such as news articles, service links or guidance documents
