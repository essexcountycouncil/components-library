import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Button } from './index.jsx';

<Meta
    title="Elements/Button"
    component={Button}
    argTypes={{
        children: {
            name: 'Content',
            description: 'Text to show to the user'
        },
        href: {
            name: 'Link',
            description: 'Where you want the button to take the user'
        },
        type: {
            name: 'Type',
            description: 'Type of button to use',
            control: {
                type: 'select',
                options: [
                    'button',
                    'text',
                    'submit'
                ]
            }
        }
    }}
    parameters={{
        jest: ['button.test.js']
    }}
/>

# Buttons

Use buttons to help users carry out an action.

<Canvas>
    <Story name="Base" args={{
        children: 'Start Application',
        href: 'financial-assessment-eligibility',
        type: 'button'
    }}>
        {(args) => <Button {...args } />}
    </Story>
</Canvas>

<Canvas>
    <Story name="Text" args={{
        children: 'Hide this message',
        type: 'text'
    }}>
        {(args) => <Button {...args } />}
    </Story>
</Canvas>

<Canvas>
    <Story name="Submit Buttons" args={{
        children: 'Continue',
        type: 'submit'
    }}>
        {(args) => <Button {...args } />}
    </Story>
</Canvas>

<ArgsTable of={Button} />

## When to use buttons

Buttons should be used to help users carry out a task, like starting an application.

Buttons should be used to:
- Start a form
- Continue to the next question of a form
- Subscribe to something, such as email notifications

## When not to use buttons

Buttons shouldn’t be used to replace links within the body of a page.

## How to use buttons

Button text should be written in sentence case and clearly tell the user what the action is. For example, ‘Start now’, ‘Continue’, ‘Subscribe’.

Having mulitple buttons on a page can confuse a user. GOV.UK recommends having one thing per page.