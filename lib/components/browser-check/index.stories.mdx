import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { BrowserCheck } from "./index.jsx";

<Meta
    title="Components/Browser Check"
    component={BrowserCheck}
    parameters={{
        actions: {
            disabled: true,
        },
        controls: {
            disabled: true,
        },
    }}
/>

# Browser check

Browser check notifies users if they are using an older browser

<Canvas>
    <Story name="Browser Check">
        <BrowserCheck />
    </Story>
</Canvas>

## When to use browser check

Use browser check on all pages of the website to ensure that no matter what page a user lands on, they are encouraged to upgrade their browser to enhance their experience.

## How to use browser check

Browser check should be placed near the start of the `<body>` tag, directly after the [skip link](/essex-county-council-digital-manual/Design-system/Elements-and-Components/skip-link) element.
